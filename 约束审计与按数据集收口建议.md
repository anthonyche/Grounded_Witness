# 约束审计与按数据集收口建议

## 1. 这一步只做什么

这一步只做了你要求的第一步：**约束审计**。

没有做的事：

- 没有重写 `Cora / MUTAG` 的全部约束
- 没有改 candidate generation
- 没有改 greedy / `UpdateWK`
- 没有改实验计划

这份文件的目标只有一个：

> 先把当前各数据集约束到底能不能解释成标准 `(P, c)`、`c` 是否真实可匹配、还有多少 multi-edge consequent、哪些路径还能支持标准 `c -> P` backchase，彻底摸清。

---

## 2. 核心结论先说

当前项目里三类数据集的状态并不一致：

### DBLP

- **可以继续作为当前主线标准实现**
- 当前默认 mined 路径已经基本符合标准 backchase
- `c` 是单边、真实、可匹配的
- DBLP 静态旧约束已经禁用

### Cora

- **当前最需要优先收口**
- 当前默认 mined 约束池里仍混有一批 multi-edge consequent
- 当前静态 Cora 约束池更严重：全部都是 multi-edge consequent
- 所以 Cora 目前**还没有完全收口到标准 `(P, c)`**

### MUTAG

- 比 Cora 好很多
- 当前默认 mined 约束池在这次审计里已经是全 single-edge consequent
- 静态 MUTAG 约束池也大体可用，但还残留一个 multi-edge consequent：`c6_closure`
- 因此 MUTAG 可以排在 Cora 之后收口

### 其他 legacy 数据集

- 当前不建议立刻重写
- 如果本轮不重跑，优先只标明 `legacy`
- 不要让它们继续污染主链判断

---

## 3. 当前各数据集约束池的结构审计

我先对仓库里当前静态约束池做了结构统计：

- `CONSTRAINTS_BASHAPE`: `total=3`, `single=2`, `multi=1`
- `CONSTRAINTS_CORA`: `total=5`, `single=0`, `multi=5`
- `CONSTRAINTS_DBLP`: `total=0`, `single=0`, `multi=0`
- `CONSTRAINTS_MUTAG`: `total=7`, `single=6`, `multi=1`
- `CONSTRAINTS_OGBN_PAPERS`: `total=10`, `single=0`, `multi=10`
- `CONSTRAINTS_TREECYCLE`: `total=5`, `single=0`, `multi=5`
- `CONSTRAINTS_YELP`: `total=9`, `single=4`, `multi=5`

这里已经能看到一个很明确的趋势：

- **DBLP 主链最干净**
- **Cora 静态池最不符合标准 `(P, c)`**
- **MUTAG 静态池基本可解释，但仍不完全干净**

---

## 4. DBLP：当前主线是否已经符合标准 backchase

### 4.1 结构层面

DBLP 当前默认配置：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/configs/local/dblp.yaml`](/Users/anthonyche/Desktop/Research/GroundingGEXP/configs/local/dblp.yaml)

关键点：

- `constraint_source: mined`
- `constraint_type_source: dblp_native_bucket`
- `constraint_rule_mode: standard_backchase`
- `constraint_filter_target_matchability: true`

DBLP 静态池已禁用：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraints.py:689`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraints.py:689)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraints.py:692`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraints.py:692)

也就是：

- `CONSTRAINTS_DBLP = []`

### 4.2 mined consequent 是否是单边 `c`

是。

关键实现位置：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:557`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:557)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:559`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:559)

这里 DBLP mined 约束是通过 `_emit_dblp_backchase_variants(...)` 从一个更大 motif 中拆出：

- 单边 consequent `c`
- 剩余边作为 antecedent `P`

所以 DBLP 当前 mined 路径，结构上已经符合标准 `(P, c)`。

### 4.3 consequent 是否真实可匹配

是，而且这一步已经有显式筛选。

关键实现位置：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:581`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:581)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:601`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:601)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:623`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:623)

当前 DBLP mined 约束会直接在 target workload 样本上测试 consequent 可匹配性，然后过滤。

### 4.4 小样本实测

我对当前 `DBLP` 默认 mined 约束做了 20 个 author workload 的 consequent 可匹配性小样本检查，结果是：

- `constraint_count = 20`
- `matchable_constraints = 20`
- `all_single_edge = true`

代表性的 consequent 命中最强约束，在 20/20 个 sampled workloads 上都能 match。

### 4.5 结论

DBLP 现在可以继续作为当前主线标准实现。  
这一点不需要回退。

---

## 5. Cora：为什么必须优先收口

### 5.1 静态 Cora 约束池

当前静态 Cora 池有 5 条约束，**全部都是 multi-edge consequent**：

- `cora_type_triangle`
- `cora_type_bridge_2_4`
- `cora_type_hub_6`
- `cora_type_bridge_3_2`
- `cora_type_bridge_1_5`

结构统计：

- `total = 5`
- `single = 0`
- `multi = 5`

这意味着：

- 静态 Cora 池**不能直接解释成标准单边 `c`**
- 它不应该继续作为标准 backchase 的主约束池

### 5.2 当前默认 mined Cora 路径

Cora 默认并不是走静态池，而是继承默认配置中的：

- `constraint_source: mined`

当前通用 mined 路径来自：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:780`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:780)
  - `_mine_triangle_constraints(...)`
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:850`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:850)
  - `_mine_extension_constraints(...)`

问题在于：

- `triangle` 模板产出的 consequent 仍然是两条边
- 只有 `extension` 模板产出的 consequent 是单边

具体证据：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:821`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:821)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:827`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/constraint_mining.py:827)

这里的 triangle mined consequent 是：

- `("A", "C")`
- `("B", "C")`

也就是一个两边 wedge，不是单边 `c`。

### 5.3 小样本 resolved 结果

我对当前默认 mined Cora 路径做了 `sigma_size=20` 的解析检查，结果是：

- `resolved = 20`
- `single = 12`
- `multi = 8`

也就是说：

- 当前默认 mined Cora 约束池**不是纯标准 `(P, c)`**
- 它是单边 consequent 与 multi-edge consequent 的混合池

### 5.4 额外问题

我还做了一个静态 Cora consequent 匹配 probe，结果发现：

- 静态 Cora 路径在 node workload 上还会受到类型标签透传问题影响
- 当前 matcher 对 node 任务要求显式 `y_type / node_labels`
- 这进一步说明静态 Cora 路径不应继续作为主链依赖

### 5.5 结论

Cora 应该是你说的**第一优先级收口对象**。  
而且建议收口方向非常明确：

- 不要先救静态 Cora 约束池
- 先把默认 mined 路径里的 multi-edge triangle consequent 去掉或改写
- 让 Cora 的默认约束池变成纯 single-edge consequent

---

## 6. MUTAG：状态比 Cora 好，但还没完全干净

### 6.1 静态 MUTAG 约束池

当前静态 MUTAG 池有 7 条：

- `c6_closure`
- `nitro_on_aromatic_completion`
- `di-oxy_on_Carbon`
- `ether_like_completion`
- `halogen_anchor_completion`
- `amine_di_carbon_completion`
- `nitro_on_nitro_completion`

结构统计：

- `total = 7`
- `single = 6`
- `multi = 1`

唯一不干净的是：

- `c6_closure`
  - `cons_edges = 2`
  - `ant_edges = 6`

这条明显不是标准单边 `c`。

### 6.2 当前默认 mined MUTAG 路径

MUTAG 也继承默认配置中的：

- `constraint_source: mined`

但和 Cora 不同，这次我对当前默认 mined MUTAG 路径做 `sigma_size=20` 解析时，结果是：

- `resolved = 10`
- `single = 10`
- `multi = 0`

也就是说：

- 当前这轮 mined MUTAG 约束恰好全部落在 single-edge extension family
- 没有混入 triangle multi-edge consequent

### 6.3 结论

MUTAG 当前状态明显比 Cora 更容易收口：

- 默认 mined 路径已经是干净的 single-edge consequent
- 只需要把静态池里的 `c6_closure` 明确隔离出去

所以你说：

1. 先 Cora  
2. 再 MUTAG  

这个优先级我同意。

---

## 7. 其他 legacy 数据集

当前静态池里：

- `Yelp`
- `BAShape`
- `OGBN_PAPERS`
- `TREECYCLE`

仍有不少 multi-edge consequent：

- `Yelp`: `9` 条里 `5` 条 multi
- `BAShape`: `3` 条里 `1` 条 multi
- `OGBN_PAPERS`: `10` 条里 `10` 条 multi
- `TREECYCLE`: `5` 条里 `5` 条 multi

如果这些数据集当前不重跑，我建议：

- 暂时先标明 `legacy`
- 不要现在就把它们混进主链判断

这和你的判断一致。

---

## 8. observed graph 共享入口之外的辅助脚本

主实验 runner 已经做到：

- observed graph 一次生成
- 全方法复用

但仓库里仍有一些**绕开主实验入口、自己直接做 masking** 的辅助脚本：

- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_cora_sensitivity.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_cora_sensitivity.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_cora_tuning.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_cora_tuning.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/regress_apx_default.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/regress_apx_default.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/compare_hybrid_candidate_generation.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/compare_hybrid_candidate_generation.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/compare_seed_ranking.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/compare_seed_ranking.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/sanity_check_candidate_generation.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/sanity_check_candidate_generation.py)
- [`/Users/anthonyche/Desktop/Research/GroundingGEXP/src/visualize_case_study.py`](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/visualize_case_study.py)

我的建议是分两类看：

### 可以优先删除或退休的

如果你要强清理主链，这几类脚本最容易删：

- `compare_seed_ranking.py`
- `compare_hybrid_candidate_generation.py`
- `sanity_check_candidate_generation.py`
- `regress_apx_default.py`

因为它们主要属于旧阶段的诊断/比较工具，不是当前主实验主链。

### 先保留但后续可重接共享入口的

- `run_cora_tuning.py`
- `run_cora_sensitivity.py`
- `visualize_case_study.py`

原因：

- 它们仍然和论文补图、case study 或 supplementary 结果有关
- 但后续应接回统一 observed graph 入口，而不是继续各自单独 masking

---

## 9. 按数据集收口的建议顺序

你给的顺序我同意，建议正式锁成：

### 优先级 1：Cora

原因：

- 还在 overall 图里
- 和 DBLP 同属 node-level，收口路径最接近
- 当前默认 mined 路径仍混有 multi-edge consequent，是当前最明显的不一致源

建议收口目标：

- 让默认 resolved Cora constraints 全部变成 single-edge consequent
- 去掉或改写 triangle mined family
- 静态 Cora 池不再参与主链

### 优先级 2：MUTAG

原因：

- graph-level case study 很重要
- 当前默认 mined 路径已经相对干净
- 只需把静态 `c6_closure` 这种非标准 consequent 单独处理

建议收口目标：

- 保持默认 mined MUTAG pool 为 single-edge consequent
- 静态 multi-edge closure 不进入主实验主链

### 优先级 3：其他 legacy 数据集

原因：

- 当前不打算重跑
- 没必要先为它们重写全部约束

建议：

- 先标明 `legacy`
- 暂不进入主链收口任务

---

## 10. 当前最重要的判断

### DBLP

- 继续作为当前主线标准实现
- 不需要回退

### Cora

- 当前还**没有**完全收口到标准 `(P, c)`
- 必须优先处理

### MUTAG

- 已经比较接近标准主链
- 但仍需把剩余 static multi-edge consequent 隔离出去

### 辅助脚本

- 主实验无关的老诊断脚本可以删
- 与 case study / tuning / supplementary 相关的脚本可以先保留，但应后续接入统一 observed graph 入口

---

## 11. 下一步最小执行建议

如果按你说的“第二步：按数据集分别收口”，我建议下一步直接这样做：

1. **先收口 Cora**
   - 目标不是立刻重写所有 Cora 约束
   - 而是先把当前默认 mined Cora pool 里的 multi-edge consequent 去掉

2. **再收口 MUTAG**
   - 默认 mined 路径先保持
   - 把 static `c6_closure` 从主链隔离出去

3. **legacy 先不动**
   - 只做标注，不做主链修复

4. **删除一批旧诊断脚本**
   - 优先删与当前主实验无关的 candidate-generation 比较脚本

---

## 12. 一句话结论

当前主线判断可以简化成一句话：

> DBLP 已经基本是标准实现；Cora 仍然混有 non-standard mined consequent，必须优先收口；MUTAG 已大体可用，但还要清掉少量 static multi-edge consequent；其余 legacy 数据集暂时只标为 legacy，不进入本轮主链修复。
