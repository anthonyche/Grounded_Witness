# 全链路最终检查与上线前验收结果

## 0. Big picture

当前项目的**主算法语义链**已经基本自洽：

- 约束统一按 `φ = (P, c)` 解释
- 标准语义统一为：
  - chase: `P -> c`
  - backchase: `c -> P`
- 自有方法 `ApxC / HeuC / Exh` 都是：
  - 先生成 witness `G_s`
  - 再做 `G_s -> G_g` 的 grounding / backchase
- baseline `GEX / PGX` 不做 `G_s -> G_g`
- witness / selected / covered 已分层
- coverage 的 `Hit_c / Active / Covered` 与 `global / normalized` 口径也已经对齐

如果只问：

> 主链语义、verification、witness、backchase、coverage 定义本身是否已经正确？

答案是：**基本正确，可以作为当前主算法主链。**

但如果问：

> 当前仓库是否已经达到“可以无歧义地开始正式实验、更新 README、并作为当前版本主链上传 GitHub”的状态？

答案是：**还差几项明确 blocker。**

这些 blocker 主要不在算法主链本身，而在：

1. `README.md` 仍是旧时代文档，严重不符当前主链
2. 根目录 `config.yaml` 现在默认是一个带 tuned DBLP balanced pool 的手动实验模板，不是中性默认配置
3. 仓库现在同时保留两条实验入口链：
   - 新的 `run_experiment_bundle.py`
   - 旧的 `run_local_experiment.py + run_local_*.sh`
   语义兼容，但对外仍不够“唯一入口”
4. 当前环境里 PyG 的可选扩展库导入失败，会污染正式 **runtime** 结论

因此：

- **可以继续做受控实验**
- **但不建议现在就更新 README 对外宣称“仓库已完全定稿”**
- **也不建议现在就把当前环境下的 runtime 结果当成最终正式数字**

---

## A. 术语与主语义层

### A1. `head/body` 与错误旧拼写检查

对当前仓库做全文扫描：

- `head`
- `body`
- `backchache`
- `H_φ`
- `H_phi`
- “最大可观测匹配”相关旧主语义关键字

结果：

- 当前仓库里**没有再检出**这些旧术语残留到当前主链代码与文档中
- 这一点说明主术语已经基本收口成功

### A2. 当前主链是否仍残留旧 trigger 逻辑

当前主链关键位置：

- [grounding_semantics.py:363](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/grounding_semantics.py:363)
- [matcher.py:154](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:154)
- [matcher.py:181](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:181)

代码显示：

1. 先在当前 witness `G_s` 上匹配 `consequent`
2. 对每个 consequent 匹配实例，抽取与 `antecedent` 有关的绑定
3. 只有 node-complete 后，才进入 repair
4. repair 调用的是：
   - `backchase_repair_cost(observed_graph, antecedent_pattern, binding, B, ...)`

这说明当前运行中的 grounding 主链已经是：

- **先 match `c`**
- **再 backchase 补 `P`**

没有发现当前主链还在用：

- 先 match `P`
- 先 match 完整目标模式
- 用“最大可观测匹配”作为标准主触发

### A3. 结论

这一层：

- **无 blocker**

---

## B. constraint pool 层

### B1. 默认 `constraint_source`

当前默认主链数据集配置：

- [dblp.yaml](/Users/anthonyche/Desktop/Research/GroundingGEXP/configs/local/dblp.yaml)
- [cora.yaml](/Users/anthonyche/Desktop/Research/GroundingGEXP/configs/local/cora.yaml)
- [mutag.yaml](/Users/anthonyche/Desktop/Research/GroundingGEXP/configs/local/mutag.yaml)

结果：

- `DBLP`: `constraint_source: mined`
- `Cora`: 默认依赖 mined 路径生成 extension 约束
- `MUTAG`: 默认依赖 mined 路径生成 extension 约束

### B2. 当前默认 resolved constraint pool 结构

我直接按当前默认配置解析了一次：

- `DBLP`: `20` 条，`20` 条都是 single-edge consequent，`0` 条 multi-edge consequent
- `Cora`: `20` 条，`20` 条都是 single-edge consequent，`0` 条 multi-edge consequent
- `MUTAG`: `9` 条，`9` 条都是 single-edge consequent，`0` 条 multi-edge consequent

### B3. consequent 可匹配性

对抽样 workload 审计结果：

- `DBLP`: `20/20` 条在抽样 workload 上至少命中过一次 `c`
- `Cora`: `20/20` 条在抽样 workload 上至少命中过一次 `c`
- `MUTAG`: `9` 条里有 `7` 条在 10 个抽样 graph workload 上命中过一次 `c`

这说明：

- `DBLP` 和 `Cora` 当前默认池，已经满足“`c` 真实可匹配、非虚拟”
- `MUTAG` 默认池也不是 synthetic / fabricated consequent
- 但 `MUTAG` graph-level 规则池的 consequent 相关性更稀，不是每条规则都会在小样本 workload 上命中

### B4. tuned rule pool 是否靠缩小规则池作弊

当前 `balanced / original / coverage_only` 这些 tuned rule pool 的设计中，仍然保持：

- candidate pool size = `64`
- final selected constraint count = `20`

没有发现：

- 靠把规则池缩到 5 或 10 来抬 coverage

### B5. 结论

这一层：

- **默认主链可用**
- `DBLP / Cora` 没有发现 synthetic consequent 混入主链
- `MUTAG` 当前默认池也不属于虚拟 consequent，但 consequent 相关性比前两者更稀

是否是 blocker：

- **不是 blocker**
- 但 `MUTAG` 的规则池还不如 `DBLP / Cora` 那么“干净且强”，属于后续可优化项

---

## C. observed graph 层

### C1. 唯一生成函数

当前主链 observed graph 生成函数是：

- node:
  - [Run_Experiment_Node.py:120](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/Run_Experiment_Node.py:120)
  - `_prepare_observed_node_workload(...)`
- graph:
  - [Run_Experiment.py:82](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/Run_Experiment.py:82)
  - `_prepare_observed_graph_workload(...)`

### C2. cache key

当前 cache key 结构里明确包含：

- dataset
- workload id
- `L`
- `max_masks`
- `mask_ratio` / incompleteness
- `random_seed`
- `preserve_connectivity`
- `constraint_signature`

这是**结构性共享**，不是单纯靠“相同 seed 大概率一样”。

### C3. bundle 入口是否做到一次生成、全方法复用

bundle 入口：

- [run_experiment_bundle.py:288](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_experiment_bundle.py:288)
- [run_experiment_bundle.py:303](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_experiment_bundle.py:303)

行为是：

1. 先 resolve constraints
2. 再 `_prewarm_observed_graphs(...)`
3. 然后所有方法读取同一批 cached observed graph

这条链满足：

- 同一 workload 的 observed graph 只生成一次
- 所有方法共享
- 生成时间在方法运行之前完成，因此不计入方法 runtime

### C4. 旧 matrix runner 是否完全等价

旧 matrix runner：

- [run_local_experiment.py](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_local_experiment.py)

它没有 bundle 那样显式 prewarm 一次，但：

- 各方法最终仍会走同一个 `_prepare_observed_*` cache key
- 因而 observed graph 本身仍会被共享

不过它还存在一个不够理想的点：

- workload 列表是按方法分别调用 `select_targets(...)` 重新算的
- 由于当前实现是固定 seed 且 deterministic，结果应一致
- 但这不是 bundle 那种“先冻结 workload list，再统一执行”的显式结构保证

### C5. incompleteness factor study

当前 observed graph cache key 包含 `mask_ratio`，因此：

- incompleteness 改变时会生成新的 observed graph
- 对同一 factor value 下所有方法仍共享同一份 graph

这一点符合要求。

### C6. 结论

这一层结论是：

- **bundle 主入口满足要求**
- **旧 matrix runner 基本可用，但还不是最干净的唯一入口实现**

是否是 blocker：

- 对“手动/受控实验”：**不是 blocker**
- 对“仓库对外宣称只有一个 canonical entrypoint”：**是中等 blocker**

---

## D. candidate / verification / witness 层

### D1. 统计分层

当前代码已经把以下几层分开：

- `candidate_count`
- `verified_witness_count`
- `selected_witness_count`
- `covered_constraint_count`

关键位置：

- [Run_Experiment_Node.py:199](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/Run_Experiment_Node.py:199)
- [Run_Experiment.py:276](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/Run_Experiment.py:276)
- [Run_Experiment.py:630](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/Run_Experiment.py:630)

### D2. witness 定义是否仍被污染

当前：

- `num_witnesses = verified_witness_count`
- 日志 `witnesses=` 也已经输出 verified 数量
- selected 与 covered 不再污染 witness 定义

这已经通过之前的定向修复和最小验证确认。

### D3. verification 基准

当前 verification 是基于：

- 当前 observed workload graph
- 当前 candidate / explanation subgraph
- 模型输出一致性

并且：

- observed graph 本身可以通过 verify
- 这也支持了“coverage=0 仍然可能有 witness”的定义

### D4. 结论

这一层：

- **无 blocker**

---

## E. window / selection 层

### E1. window 未满时是否允许 verified witness 入窗

当前三个自有方法都已修正：

- [apxchase.py:1378](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/apxchase.py:1378)
- [heuchase.py:691](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/heuchase.py:691)
- [exhaustchase.py:702](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/exhaustchase.py:702)

行为：

- 当 `window` 未满时，verified witness 会直接 admit
- 不再要求先有 grounded coverage

### E2. 是否还残留“必须先有 coverage 才能入窗”

当前没有在 window-not-full 路径上发现这类条件。

在 window 已满后：

- replacement 仍然要求 set objective 改进
- 这是允许保留的，因为它已经是“后续优化层”，不是 witness 合法性层

### E3. 结论

这一层：

- **无 blocker**

---

## F. grounding / backchase 层

### F1. 是否真的是 `c -> P`

关键位置：

- [grounding_semantics.py:365](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/grounding_semantics.py:365)
- [grounding_semantics.py:376](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/grounding_semantics.py:376)
- [matcher.py:181](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:181)

当前实现是：

1. 先对 `consequent` 找匹配
2. 再对 `antecedent` 做 node-complete 检查
3. 再在 witness 节点域内 repair `antecedent`

### F2. node-complete gating

存在，而且位置明确：

- [grounding_semantics.py:378](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/grounding_semantics.py:378)

逻辑：

- 如果 `assigned_count != antecedent_var_count`
- 则该 consequent match 不进入 active / repair

### F3. repair 是否只允许在 witness 节点域内补边

是。

关键位置：

- [matcher.py:224](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:224)
- [matcher.py:319](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:319)

逻辑：

- 候选节点会被限制在 `witness_nodes`
- 任何 edge pair 搜索也要求两端节点都在 `witness_nodes`

这符合：

- 缺边可以补
- 缺点不可以补

### F4. supporting / grounded edge / ΔE

当前定义仍然正确：

- supporting edge：
  - 在 observed graph 中存在
  - 不在当前 witness 中
  - 不计入 `ΔE`
- grounded edge：
  - 不在 observed graph 中
  - 计入 `ΔE`

关键位置：

- [matcher.py:253](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:253)
- [matcher.py:259](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/matcher.py:259)
- [grounding_semantics.py:423](/Users/anthonyche/Desktop/Research/GroundingGEXP/src/grounding_semantics.py:423)

### F5. `G_g` 是否写回 observed graph

没有发现这类写回。

当前逻辑是：

- grounded 结果通过 metadata 挂在 witness 上
- `observed_graph` 本身不被增边改写

### F6. local budget 是否真的是“每个 consequent 匹配实例一次 repair”

是。

关键证据：

- `evaluate_grounding(...)` 会遍历每个 consequent match 实例
- 每个实例都独立调用一次 `backchase_repair_cost(..., B, ...)`
- 只要存在一个 `within_budget` 的实例，该约束就进入 `grounded_names`

这满足：

- budget `k` 是 local，不是整条约束共享，不是 workload 共享
- cover 判定是 “exists one feasible repair under local budget”

### F7. 结论

这一层：

- **无 blocker**

---

## G. metric 输出层

### G1. per-workload 字段

bundle per-workload 现在已经稳定输出：

- `candidate_count`
- `verified_witness_count`
- `selected_witness_count`
- `covered_constraint_count`
- `coverage_global`
- `coverage_normalized`
- `conciseness`
- `fidelity_minus`
- `runtime_method_only`
- `status`

关键位置：

- [run_experiment_bundle.py:202](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_experiment_bundle.py:202)

### G2. method summary 字段

当前 method summary 也已经稳定聚合：

- `avg_candidate_count`
- `avg_verified_witness_count`
- `avg_selected_witness_count`
- `avg_covered_constraint_count`
- `avg_coverage_global`
- `avg_coverage_normalized`
- `avg_conciseness`
- `avg_fidelity_minus`
- `runtime_total`
- `runtime_per_workload`

关键位置：

- [run_experiment_bundle.py:274](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_experiment_bundle.py:274)

### G3. collect 链兼容性

`collect_results.py` 已经能读取：

- `verified_witness_count`
- `selected_witness_count`
- `covered_constraint_count`

并保持旧字段 `num_witnesses` 兼容，但语义已明确等于 verified。

### G4. 结论

这一层：

- **无 blocker**

---

## H. 脚本入口层

### H1. 当前活跃入口链

当前实际存在两条入口链：

#### 1. 新 bundle 主入口

- [run_experiment_bundle.py](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_experiment_bundle.py)

优点：

- 单 config
- factor sweep
- 前置 observed graph prewarm
- workload 级 / method 级 CSV 稳定输出
- 更符合当前主链定义

#### 2. 旧 matrix runner 链

- [run_local_experiment.py](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_local_experiment.py)
- [run_local_efficiency.sh](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_local_efficiency.sh)
- [run_local_effectiveness.sh](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_local_effectiveness.sh)
- [run_local_all.sh](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/run_local_all.sh)

优点：

- 还能跑整批 full configs

不足：

- 没有 bundle 那么清晰的 workload prewarm / bundle-level metadata
- 对 workload list 的共享不如 bundle 那样显式

### H2. plot / collect 兼容性

- [collect_results.py](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/collect_results.py)
- [plot_full_experiments.py](/Users/anthonyche/Desktop/Research/GroundingGEXP/scripts/plot_full_experiments.py)

两者仍能用，但：

- plotting 主要面向 full matrix runner 产物
- README 里并没有正确说明这条新旧关系

### H3. 结论

这一层的主要问题不是“脚本坏了”，而是：

- **当前仓库还没有真正对外只暴露一个唯一 canonical experiment entrypoint**

这会导致：

- 内部能用
- 但对外说明不够干净

是否是 blocker：

- 对内部受控实验：**不是 blocker**
- 对 README 更新 / GitHub 上线：**是 blocker**

---

## I. overall / factor 实验准备状态

### I1. overall 三个实验是否具备重新执行条件

当前已经具备：

- Efficiency
- Coverage
- Fidelity

前提是：

- 先锁定当前使用的 constraint pool 模式
- 特别是 `DBLP` 是否用：
  - `original`
  - `balanced`
  - `coverage_only`

### I2. DBLP factor study 是否具备逐个执行条件

具备。

原因：

- 主链语义正确
- coverage / fidelity / conciseness 输出链正确
- observed graph 共享机制正确
- rule pool 也已有 `original / balanced / coverage_only` 可切换

### I3. 当前还缺什么 blocker

见下一节“blocker 列表”。

---

## 当前 blocker 列表

以下问题会**直接影响正式实验结论、对外说明，或仓库作为当前主线版本的可信度**。

### Blocker 1: `README.md` 严重过时且与当前主链不一致

当前 [README.md](/Users/anthonyche/Desktop/Research/GroundingGEXP/README.md) 里仍然存在：

- 旧入口：
  - 直接运行 `src/Run_Experiment_Node.py`
- 旧方法名：
  - `ApxIChase / HeuIChase / Exhaustive naivechase`
- 旧参数口径：
  - `Budget: 8`
  - `exp_name: heuchase_mutag`
- 旧数据集叙事：
  - `BAShape / Yelp / OGBN-Papers100M` 等老路径
- 旧输出目录：
  - `results/`
- 已删除旧脚本：
  - `Plot_Figures_2.py`

影响：

- 对外阅读 README 的人会得到错误的实验入口、错误的参数语义、错误的当前主链认知

严重程度：

- **高**

最小修复建议：

- 先不要更新 README 为对外版本，直到重写为当前 bundle / mainline 口径

### Blocker 2: 根目录 `config.yaml` 当前不是“中性默认配置”

当前 [config.yaml](/Users/anthonyche/Desktop/Research/GroundingGEXP/config.yaml) 里默认是：

- `dataset: dblp`
- `constraint_pool_mode: balanced`
- `exhaustchase_timeout_sec: 1800`
- 固定 30 个 DBLP target nodes

这不是一份“中性的默认 config”，而是一份：

- 手动 DBLP regression 配置
- 还是 tuned balanced pool 配置

影响：

- 如果用户不注意，直接按 README 或直觉运行 `config.yaml`
- 得到的是 tuned DBLP balanced 结果，而不是 neutral default

严重程度：

- **高**

最小修复建议：

- 要么把 `config.yaml` 改成真正中性的 `manual_template`
- 要么明确把当前文件改名成 `manual_dblp_balanced.yaml`
- 并让根目录 `config.yaml` 只保留最小默认模板

### Blocker 3: 当前仓库仍对外暴露两条实验入口链

目前既有：

- `run_experiment_bundle.py`
- 也有 `run_local_experiment.py + run_local_*.sh`

它们语义兼容，但不够“唯一入口”。

影响：

- 对外文档无法清楚回答“正式实验到底该用哪条链”
- 也容易让未来用户误走旧 matrix runner 口径

严重程度：

- **中高**

最小修复建议：

- 对外只推荐 bundle 链
- 或者让 `run_local_*` 进一步包到 bundle 上

### Blocker 4: 当前环境里 PyG 可选扩展库损坏，会污染正式 runtime 结论

当前环境反复出现：

- `torch-scatter` import warning
- `torch-sparse` import warning
- `torch-cluster` import warning
- `torch-spline-conv` import warning

这说明：

- 当前运行环境会退化到不含这些优化库的路径
- 语义一般不变
- 但运行时间可能明显受影响

影响：

- **会污染正式 runtime 实验结论**
- 尤其对 `Exh` 这种本来就很重的方法影响更大

严重程度：

- **高（对 runtime claim）**

最小修复建议：

- 在修好 PyG 扩展环境之前，不要把当前机器上的 runtime 数字当最终正式结果

---

## Non-blocking cleanup 列表

以下问题目前不会直接破坏主算法结论，但最好在后续继续清：

1. 非主链辅助脚本仍各自调用 masking / observed graph 准备
   - 例如某些 compare / visualize 类脚本
   - 不影响主实验，但容易造成旁路理解污染

2. `torch.load(weights_only=False)` 的 future warning
   - 不是当前实验 blocker
   - 但安全与未来兼容性上应该收口

3. `MUTAG` 默认规则池虽然是 single-edge consequent，但在小样本 graph workload 上并非每条规则都常常命中
   - 不属于 synthetic
   - 但 graph-level 规则质量还可继续优化

4. plotting 仍依赖 `legacy_overall_reference.csv` 合并 legacy overall 图
   - 这是预期能力
   - 但对外文档里应明确说明 legacy merge 是可选输入，而非仓库强依赖

---

## 是否可以开始正式实验

### 可以的部分

如果你现在做的是：

- 内部受控实验
- 且明确锁定 config
- 且优先使用 `run_experiment_bundle.py`
- 且把当前环境 runtime 只当作 provisional

那么：

- **可以开始正式实验准备与分批执行**

### 不建议直接做的部分

如果你现在要做的是：

- 对外宣称仓库已经 fully stabilized
- 立即更新 README 作为公共说明
- 立即把当前环境的 runtime 结果当正式数字

那么：

- **还不建议**

---

## 当前是否可以更新 README

**不可以。**

原因不是主链还错，而是：

- 当前 README 仍然严重错误
- 一旦更新 README，就等于对外承诺当前主线入口和语义
- 但当前入口链和默认 config 还没有收成“唯一且无歧义”的样子

先做最小剩余修复项，再更新 README 才稳。

---

## 当前是否可以上传 GitHub

### 如果标准是“代码主链语义是否基本可接受”

- **可以**

### 如果标准是“作为当前仓库主版本对外展示是否已经足够专业、清晰、不会误导”

- **还不建议立即 push 为对外主版本**

原因：

- README 错
- 默认 config 不中性
- 双入口链仍并存
- runtime 环境仍不干净

---

## 最小剩余修复项

按优先级建议：

1. **重写 README**
   - 只讲当前主链
   - 只讲 `(P, c)` 与标准 backchase
   - 只讲 bundle 入口或明确 bundle 为 canonical entrypoint

2. **处理中性默认 config**
   - 把当前根目录 `config.yaml` 改成真正中性的手动模板
   - 把 tuned `balanced` 版本另存为显式命名文件

3. **统一入口对外口径**
   - 明确：
     - bundle 是唯一推荐主入口
   - 旧 matrix runner 只保留为内部兼容或 legacy automation

4. **修运行环境**
   - 解决 PyG optional ops 版本不兼容
   - 再谈正式 runtime 结论

---

## 最终结论

一句话结论：

> 当前项目已经基本达到“主链语义正确、统计定义正确、observed graph 公平共享、constraint pool 主线干净”的状态，可以继续做受控正式实验；但还没有达到“可以直接对外更新 README 并作为稳定主版本上传 GitHub”的状态。真正的剩余 blocker 主要是 README、默认 config 的非中性、双实验入口并存，以及当前环境会污染 runtime 结论。  
